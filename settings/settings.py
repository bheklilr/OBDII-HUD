"""
Needs to implement a settings daemon that can quickly change from one settings
file to another, since the plan is to have it so that the GUI layout can be
changed on the fly (i.e. different profiles and ease of use)
"""

import parser
